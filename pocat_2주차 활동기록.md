# PoCaT 프로젝트 2주차 활동 기록

- 날짜: 2026-08-29 (토)
- 세션 시간: 4시간 중 진행
- 오늘의 역할: Claude가 "코딩 과외 선생님" 모드로 진행 (완성 코드를 먼저 주지 않고, 질문으로 유도 → 직접 작성 → 오류 스스로 발견/수정 방식)

---

## 1. 실력 진단 (Diagnostic)

과거 이력(ADsP 자격증, 파이썬/SQL 강의 수강)만으로 실력을 판단하지 않고, 직접 코드를 짜보게 해서 확인함.

### 문제 1: 리스트에서 짝수만 골라 새 리스트 만들기

- 처음 시도: `for i in numbers: if numbers[i] % 2 == 0: new_list += numbers[i]`
  - 오류 1: `for i in numbers`에서 `i`는 인덱스가 아니라 값 자체인데 `numbers[i]`로 다시 인덱싱해서 잘못된 값 참조 → 본인이 `print(i)`로 확인 후 스스로 `numbers[i]` → `i`로 수정
  - 오류 2: `new_list += i` (int를 리스트에 `+=`) → `TypeError: 'int' object is not iterable`. 본인이 "자료형 문제 같다"고 정확히 진단. `append()`를 아는지 질문 → 스스로 `append(i)`로 답을 맞힘
- 최종 정답: `new_list.append(i)` 사용, 결과 `[8, 22, 10, 4]` 정확히 도출
- 추가 확인: `else: pass`가 있으나 없으나 결과가 같은 이유(=`pass`는 아무 동작도 안 하는 자리채움 명령어, `if`만 있고 `else`가 없으면 조건이 거짓일 때 그냥 건너뜀)까지 정확히 이해

### 문제 2: 딕셔너리 리스트에서 조건에 맞는 값 뽑기

```python
players = [
    {"name": "김선수", "team": "LG", "avg": 0.320},
    {"name": "이선수", "team": "두산", "avg": 0.280},
    {"name": "박선수", "team": "LG", "avg": 0.295},
]
```
team이 "LG"인 선수의 name만 뽑기.

- 처음 시도: `for`문 없이 `players_team.value`, `players_name.value` 같은 존재하지 않는 문법 사용 → 딕셔너리를 점(`.`) 표기로 접근하려 함
- 새 개념 설명: 딕셔너리는 대괄호(`["key"]`)로 접근한다는 것, 리스트 안 딕셔너리를 하나씩 꺼내려면 `for`문이 필요하다는 것을 설명
- 재시도: `for i in players: if (i["team"] ="LG") ...` → `=`와 `==` 혼동, 콜론(`:`) 누락 → 본인이 실행 후 두 개 다 스스로 찾아 수정
- 최종 정답: `new_list.append(i["name"])`, 결과 `['김선수', '박선수']` 정확히 도출
- `=`(대입)과 `==`(비교)이 "조건문 안이라 상관없는 것"이 아니라 파이썬 문법상 원래부터 완전히 다른 것이라는 점 짚어줌

### 문제 3: 함수 + return (타율로 등급 매기기)

`avg`를 받아서 0.3 이상이면 `"주전급"`, 아니면 `"보통"` 리턴하는 함수.

- 처음 시도: 함수 매개변수 `avg`를 안 쓰고, 함수 밖에 있던 `players` 리스트를 다시 끌어와 `for`문으로 순회 → **매개변수(parameter) 개념 자체가 약함**을 확인. 스스로 "매개변수 avg를 아예 신경 안 썼다"고 정확히 자기 진단함
- 재작성: `avg` 하나만 받아서 조건 판단하도록 수정 → 로직은 맞았으나 `print()` 없이 `return`만 써서 결과가 안 보임 → `return`(함수 밖으로 결과 전달)과 `print`(화면 출력)의 차이를 설명
- 그 다음엔 셀을 실행(Shift+Enter)하지 않은 상태였던 실수도 있었음 (실행 경험 자체가 아직 적음)
- 최종적으로 `avg >= 0.3` 조건과 `print(get_grade(0.4))`까지 정확히 완성

### 문제 4: 파일 쓰기/읽기 (json)

`players`를 `players.json`으로 저장하고 다시 읽어오기.

- `open()`, `with ... as f:`, `json.dump()`, `json.load()`를 이번에 처음 배움 (완전히 모른다고 솔직히 인정 후 개념 설명 받음)
- 시행착오:
  1. `json.dump(players = [...], players.json)` → 리스트를 다시 통째로 쓰려 하고, `players.json`을 파일 객체 대신 속성처럼 씀 → `SyntaxError: positional argument follows keyword argument`. 이미 만들어둔 변수 `players`를 그대로 쓰면 된다는 것, 파일 객체는 `open()`으로 연 `f`를 써야 한다는 것 설명
  2. `"w"`(쓰기 전용) 모드로 연 파일에서 바로 `json.load()`(읽기) 시도 → `UnsupportedOperation: not readable`. 쓰기/읽기 모드가 다르다는 걸 스스로 추론해서 `with` 블록을 쓰기용/읽기용으로 분리
  3. `json.load(f)`를 두 번 호출(한 번은 결과 버림, 한 번은 print 안에) → 두 번째 호출에서 `JSONDecodeError: Expecting value`. 파일은 "책갈피"처럼 한 번 읽으면 커서가 끝으로 이동한다는 개념 설명 → 결과를 변수에 한 번만 담도록 수정
- 최종 정답: 정상적으로 저장 후 읽어서 출력 성공

---

## 2. 진단 결과 종합 평가

**강점**
- 에러 메시지(Traceback)를 읽고 원인을 스스로 추론하는 능력이 좋음
- 한 번 새 개념을 설명받으면 바로 다음 코드에 정확히 적용, 같은 유형 실수 반복 안 함
- 모르는 걸 "모르겠다"고 솔직히 인정하고 넘어감

**약점 (몸에 덜 붙은 기초 문법)**
- `for` 반복문에서 값과 인덱스를 헷갈림
- `=`/`==` 혼동, 콜론 누락 등 기본 문법 실수가 잦음
- 딕셔너리 접근 문법(`[]`)을 몰랐음
- 함수 매개변수(scope) 개념이 약함 — 정의한 매개변수를 안 쓰고 바깥 변수를 참조하려는 경향
- 파일 입출력은 완전히 처음, 코드 셀 실행(Shift+Enter) 등 실습 경험 자체가 적음

**종합**: 개념은 어느 정도 알지만 손으로 직접 짜는 근육 기억이 약한 전형적인 "이론은 들었지만 실습이 적은" 상태. 다만 세션 내 학습 속도는 빠른 편(힌트 1~2번이면 이후 반복 안 함).

---

## 3. Streamlit 첫 화면 만들기

목표: `players.json` 데이터를 브라우저 화면에 띄우기 (필터/정렬은 다음으로 미룸).

- 새 개념: Streamlit은 파이썬 스크립트를 위에서 아래로 쓰면 그대로 웹 화면 요소로 변환해주는 도구. 오늘 쓴 함수: `st.title()`, `st.write()`
- 시행착오:
  1. `import streamlit,json` 이후 코드에서는 `st.title(...)`처럼 별명(`st`)을 썼는데 `import streamlit as st`로 안 쓴 것 발견 → `import ... as 별명` 개념 설명
  2. `players` 변수를 실제로 `json.load(f)`로 읽어서 담는 과정이 빠져 있던 것도 스스로 찾아 수정 (문제 4에서 배운 걸 응용)
- 최종 `app.py`:
```python
import streamlit as st
import json
with open("players.json","w") as f:
    players = [
        {"name":"박찬호","team":"LA다저스","avg":0.289},
        {"name":"이승엽","team":"삼성라이온즈","avg":0.302},
        {"name":"류현진","team":"토론토블루제이스","avg":0.275}
    ]
    json.dump(players,f)
with open("players.json","r") as f:
    i_read = json.load(f)
    st.title("선수 정보")
    for i in i_read:
        st.write(i["name"],i["team"],i["avg"])
```
- `streamlit run app.py` 실행 → 브라우저 첫 로딩 시 빈 화면 문제 있었으나 새로고침으로 해결 (실제 코드 문제 아니었음)
- 결과: `http://localhost:8501`에서 선수 3명(박찬호, 이승엽, 류현진) 정보가 화면에 정상 출력됨
- 추가 개념 확인: Streamlit은 새로고침/상호작용마다 `app.py` 전체를 처음부터 다시 실행한다는 핵심 동작 원리를 스스로 유추하고 이해함 (이후 버튼/필터 추가 시 중요한 개념)

---

## 4. 오늘 새로 배운 개념 (한 줄 정리)

- **for 반복문의 i는 인덱스가 아니라 값**: `for i in list`는 리스트 안 값을 하나씩 꺼내주는 것
- **딕셔너리 접근**: `dict["key"]`로 값을 꺼낸다 (점 표기 아님)
- **list.append(값)**: 리스트에 값 하나를 끝에 추가하는 전용 문법
- **함수 매개변수(parameter)**: 함수 안에서는 넘겨받은 매개변수만 가지고 판단해야 하며, 함수 밖 변수를 끌어다 쓰면 안 됨
- **return vs print**: `return`은 함수 밖으로 결과를 돌려주는 것, `print`는 화면에 실제로 보여주는 것 (별개의 동작)
- **open() / with ... as f: / json.dump() / json.load()**: 파일을 열고, 자동으로 닫아주는 안전장치(`with`)를 쓰고, json 형식으로 저장/불러오기
- **파일은 한 번 읽으면 커서가 끝으로 이동**: `json.load(f)`를 두 번 호출하면 두 번째는 빈 내용을 읽으려다 에러
- **encoding="utf-8"**: 한글 등 문자를 안전하게 저장/읽기 위한 표준 인코딩 지정
- **import 모듈 as 별명**: 긴 모듈 이름을 짧게 불러 쓰기 위한 문법 (예: `import streamlit as st`)
- **Streamlit의 재실행 모델**: 새로고침/상호작용마다 스크립트 전체가 처음부터 다시 실행됨

---

## 5. 아직 이해가 부족해서 다음에 복습이 필요한 것

- 기본 문법 정확도(콜론, `=`/`==`, 인덱스 vs 값)는 알고는 있지만 손에 완전히 붙지는 않은 상태 — 반복 연습 필요
- 함수의 매개변수/스코프 개념은 한 번 짚었지만, 더 복잡한 함수(여러 매개변수, 여러 조건)에서 다시 헷갈릴 가능성 있음 — 다음에 재확인 필요

## 6. 다음 시간(3주차 예정)에 할 것

- 오늘 만든 기본 화면에 실제 KBO 데이터(팀 순위 또는 선수 스탯)를 반영
- 순수 파이썬으로 필터링/정렬 로직 작성 연습 (pandas 없이) — 이 부분이 이번 프로젝트의 핵심 학습 목표이자 시간이 더 걸릴 수 있는 영역
- browser-use 데이터 수집은 아직 시작 전 — 별도 세션에서 진행 예정 (API 비용/속도 이슈 있을 시 requests+BeautifulSoup 대안 논의)

## 7. 실력 변화 (진단 대비)

오늘 세션 자체가 1주차 진단을 겸했기 때문에 "이전 대비 변화"는 아직 비교 대상이 없음. 다만 세션 안에서 관찰한 바로는, 새 개념을 1~2번 설명 들으면 바로 다음 시도에 정확히 적용하는 학습 속도를 보였음 — 앞으로 매주 이 패턴이 유지되는지 지켜볼 필요 있음.
