# PoCaT 프로젝트 4주차 활동 기록

- 날짜: 2026-09-19 (3주차로부터 2주 뒤)
- 오늘의 역할: 사용자가 시간이 부족하다고 요청 → 이번 세션은 Claude가 직접 구현 위주로 진행. 데이터 수집은 원래도 학습 목표가 아니었던 부분이라 방향 자체는 기존 원칙과 크게 다르지 않음.

---

## 1. 선수 전체 데이터 확보 (3주차부터 이어진 최우선 과제, 완료)

- 문제: `hitters.json`이 리그 전체 타율 상위 30명만 담고 있었음 (koreabaseball.com 선수기록 페이지가 ASP.NET postback 기반이라 2페이지 이후 접근이 막혀 있었음).
- 지난 시도가 실패했던 이유를 재확인: postback 요청에 `__VIEWSTATE` 등 3개 필드만 보내고 `hfPage`, `hfOrderByCol`, `hfOrderBy` 같은 다른 hidden 필드들을 안 보내서 서버가 에러 페이지를 반환했던 것으로 확인됨.
- **해결 방식 변경**: 전체 목록의 페이지를 넘기는 대신, **팀별 필터(`ddlTeam` 드롭다운) postback으로 10개 팀을 하나씩 요청** → 각 팀 결과 안에서도 30명 넘으면 페이지네이션(`btnNo2`, `btnNo3`...) 추가로 따라가도록 구현.
- 페이지 전환 시 매번 최신 `__VIEWSTATE`(서버가 응답마다 갱신)를 다시 캡처해서 다음 요청에 실어 보내야 한다는 것이 핵심이었음.
- 결과: **30명 → 287명**으로 확장 (팀당 24~34명, AVG가 "-"인 타석 없는 선수는 제외).
- 중복 1건 발견 → `(이름, 팀)` 기준으로 팀별 dedup 로직 추가해서 해결.
- 기존 `app.py`는 `hitters.json` 스키마(name/team/avg/g/hr/rbi)를 그대로 유지했기 때문에 코드 수정 없이 그대로 작동 확인.
- GitHub에 커밋/푸시 완료 → Streamlit Community Cloud 자동 재배포됨.

## 2. 새로고침 버튼 Node→Python 전환 (완료)

- 배포 사이트에서 새로고침 버튼을 눌렀더니 실제로 `FileNotFoundError`(node 명령어 없음)가 발생하는 걸 사용자가 직접 확인 → 예정보다 앞당겨서 바로 수정.
- `kbo-game`(Node) 패키지 소스를 보니 실제로는 `koreabaseball.com/ws/Main.asmx/GetKboGameList`에 단순 JSON POST 요청 하나 보내는 것뿐이었음 → 파이썬 `requests`로 동일하게 재구현(`fetch_games.py`).
- `app.py`에서 `subprocess.run(["node", ...])` 대신 `from fetch_games import fetch_games` 후 직접 함수 호출로 변경 — subprocess 자체가 필요 없어짐(더 단순해짐).
- `fetch_games.mjs`(Node) 삭제, Node.js 의존성 완전히 제거. 로컬 테스트 통과 후 배포 완료.

## 2-1. 스탯 확장 (완료, 2시간 내 마감 요청으로 Claude가 직접 구현)

- 사용자 요청: 타율 말고 스탯이 더 있었으면 좋겠다 + 투수 기록도 + 선수 프로필(사진/생년월일/포지션)도.
- `fetch_hitters.py`: 안타/2루타/3루타/홈런/타점/희생타 등 컬럼 추가, 선수별 `playerId`도 함께 저장(상세보기용).
- `fetch_pitchers.py` 신규 작성: 투수 기록 페이지도 타자와 같은 팀별 postback+페이지네이션 방식으로 288명 확보.
- `player_detail.py` 신규: 선수 상세페이지(`HitterDetail`/`PitcherDetail`)에서 사진 URL, 생년월일, 포지션, 신장/체중, 경력을 긁어오는 함수. 287+288명 전체를 미리 긁지 않고, 화면에서 **선택한 선수 1명만 그때그때 실시간으로 가져오는 방식**(lazy fetch)으로 설계해서 시간/요청 수를 아낌.
- `app.py` 구조 재정리: "오늘의 KBO 경기" / "팀별 선수 기록(타자+투수)" / "선수 상세보기(사진 포함)" 3개 섹션으로 재구성. 2주차 연습용 더미 데이터(`players.json`, 박찬호 등) 섹션은 완성본에서 제외함.
- 참고: 상세보기가 선택될 때마다 KBO 사이트에 실시간 요청을 보내는 구조라, 이미 합의된 "작은 개인 프로젝트" 판단 하에 진행함(3주차 robots.txt 논의 참고).

## 3. 아직 남은 것 (다음 세션)

- 시각화 재시도 여부 — 3주차 막대그래프는 롤백된 상태 그대로.
- 정렬 로직(`sorted()`) 등 순수 파이썬 학습 과제는 이번 세션에도 다루지 못함 — 시간 부족으로 계속 다음으로 이월 중. 다음 세션엔 우선적으로 다뤄야 함.

## 3. 참고

- 이번 세션은 시간 제약으로 사용자가 직접 코드를 짜는 단계를 건너뛰었음. 다음 세션에서 위 항목들을 진행할 때는 원래 방식(설명 → 순서 안내 → 직접 작성 → 질문으로 유도)으로 복귀.
