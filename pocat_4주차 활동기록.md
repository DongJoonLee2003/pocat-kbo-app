# PoCaT 프로젝트 4주차 활동 기록

- 날짜: 2026-09-19 (3주차로부터 2주 뒤)
- 오늘의 역할: 사용자가 시간이 부족하다고 요청 → 이번 세션은 Claude가 직접 구현 위주로 진행. 데이터 수집은 원래도 학습 목표가 아니었던 부분이라 방향 자체는 기존 원칙과 크게 다르지 않음.

---

## 1. 선수 전체 데이터 확보 (3주차부터 이어진 최우선 과제, 완료)

- 문제: `hitters.json`이 리그 전체 타율 상위 30명만 담고 있었음 (koreabaseball.com 선수기록 페이지가 ASP.NET postback 기반이라 2페이지 이후 접근이 막혀 있었음).
- 지난 시도가 실패했던 이유를 재확인: postback 요청에 `__VIEWSTATE` 등 3개 필드만 보내고 `hfPage`, `hfOrderByCol`, `hfOrderBy` 같은 다른 hidden 필드들을 안 보내서 서버가 에러 페이지를 반환했던 것으로 확인됨.
- **해결 방식 변경**: 전체 목록의 페이지를 넘기는 대신, **팀별 필터(`ddlTeam` 드롭다운) postback으로 10개 팀을 하나씩 요청** → 각 팀 결과 안에서도 30명 넘으면 페이지네이션(`btnNo2`, `btnNo3`...) 추가로 따라가도록 구현.
- 페이지 전환 시 매번 최신 `__VIEWSTATE`(서버가 응답마다 갱신)를 다시 캡처해서 다음 요청에 실어 보내야 한다는 것이 핵심이었음.
- 결과: **30명 → 287명**으로 확장 (팀당 24~34명, AVG가 "-"인 타석 없는 선수는 제외).
- 중복 1건 발견 → `(이름, 팀)` 기준으로 팀별 dedup 로직 추가해서 해결.
- 기존 `app.py`는 `hitters.json` 스키마(name/team/avg/g/hr/rbi)를 그대로 유지했기 때문에 코드 수정 없이 그대로 작동 확인.
- GitHub에 커밋/푸시 완료 → Streamlit Community Cloud 자동 재배포됨.

## 2. 아직 남은 것 (다음 세션)

- 새로고침 버튼이 배포 환경(Streamlit Cloud, Node.js 없음)에서 작동 안 하는 문제 — `fetch_games.mjs`를 파이썬으로 재작성해야 함. 이번엔 손 못 댐.
- 시각화 재시도 여부 — 3주차 막대그래프는 롤백된 상태 그대로.
- 정렬 로직(`sorted()`) 등 순수 파이썬 학습 과제는 이번 세션엔 다루지 못함 — 시간 부족으로 다음으로 이월.

## 3. 참고

- 이번 세션은 시간 제약으로 사용자가 직접 코드를 짜는 단계를 건너뛰었음. 다음 세션에서 위 항목들을 진행할 때는 원래 방식(설명 → 순서 안내 → 직접 작성 → 질문으로 유도)으로 복귀.
