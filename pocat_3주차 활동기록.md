# PoCaT 프로젝트 3주차 활동 기록

- 날짜: 2026-09-05 (토)
- 오늘의 역할: Claude가 "코딩 과외 선생님" 모드로 진행 (완성 코드를 먼저 주지 않고, 질문으로 유도 → 직접 작성 → 오류 스스로 발견/수정 방식). 단, 데이터 수집 스크립트(Node.js, Python requests+BeautifulSoup)와 새로고침 버튼(subprocess)은 학습 목표가 아니라서 Claude가 작성.

---

## 1. 오늘의 목표 변화

세션은 "오늘 KBO 경기 결과가 궁금하다"는 가벼운 질문에서 시작했다가, 그 결과를 실제로 웹사이트에 반영하고 싶다는 요청으로 자연스럽게 확장됨. 최종적으로 오늘 다룬 범위:

1. 오늘 KBO 경기 목록/스코어를 `app.py`에 표시
2. 경기 상태(예정/진행중/종료/취소)에 따라 다르게 보여주기
3. 새로고침 버튼으로 최신 데이터 재수집
4. 팀별 선수 명단 + 타율을 라디오 버튼으로 골라서 보기

## 2. 데이터 수집 방식 결정 (사용자와 상의)

- 경기 결과: `kbo-game`이라는 npm(Node.js) 패키지로 가져오기로 결정. 원래 계획(browser-use)과 다른 방식이라 진행 전 사용자에게 먼저 확인받음 → 승인.
- 선수 타율: 이 세션에는 애초에 `browser-use` 툴 자체가 연결되어 있지 않다는 것을 확인. 조용히 다른 방법으로 넘어가지 않고, `requests + BeautifulSoup`로 전환할지 사용자에게 먼저 확인받음 → 승인.
- KBO 공식 사이트(`koreabaseball.com`)의 선수 기록 페이지가 ASP.NET 기반이라 2페이지 이후는 postback(POST 요청) 방식이라 접근이 까다로움 → 1페이지(타율 상위 30명, 10개 팀 전원 최소 2명 이상 포함)만 우선 사용하기로 결정. 전체 로스터가 아니라 "상위 타율 30명"이라는 제한이 있음.

## 3. Streamlit 화면 작업 — 경기 결과

목표: `games.json`을 읽어서 오늘 경기 목록을 화면에 띄우기 (2주차에서 `players.json`으로 연습한 패턴 재사용 + 새 요소 추가).

- 새 개념: **중첩 딕셔너리** — `games.json`의 `score` 필드는 `{"home":0,"away":0}`처럼 딕셔너리 안에 딕셔너리가 있는 구조. `game["score"]["home"]`처럼 대괄호를 두 번 써야 값이 나온다는 것을 처음 배움.
- 1차 시도에서 나온 오류들 (질문으로 유도해서 전부 스스로 수정):
  - `open()`/`json.load()` 호출 결과를 변수에 담지 않음
  - `for i in game:`에서 `game`이 정의된 적 없는 변수
  - `return`을 함수 밖(최상위 코드)에서 사용 → 2주차에서 배운 "return은 함수 안에서만" 개념을 다시 확인
  - 딕셔너리 키에 따옴표 없이 접근 (`game[stadium]` → `game["stadium"]`이어야 함)
  - f-string에 중괄호 `{}` 없이 써서 변수가 아니라 글자 그대로 출력될 뻔함 (f-string 개념 이번에 처음 배움)
- 실제 실행 중 `UnicodeDecodeError: 'cp949' codec can't decode...` 발생 → 2주차에서 배운 `encoding="utf-8"`을 `games.json`을 열 때 빠뜨렸다는 것을 스스로 찾아 수정. **이후 `hitters.json`을 열 때는 힌트 없이 처음부터 `encoding="utf-8"`을 직접 넣음 — 완전히 체화됨.**
- 최종적으로 상태(`status`)에 따라 4가지(`SCHEDULED`/`IN_PROGRESS`/`FINISHED`/`CANCELED`)를 각각 다른 문구로 분기하는 코드를 요청한 것(if/else 2갈래)보다 더 꼼꼼하게(if/elif 4갈래) 완성함.

## 4. 새로고침 기능 (Claude 구현)

- "경기가 시작되면 실시간으로 반영됐으면 좋겠다"는 요청에 따라, 버튼을 누르면 데이터 수집 스크립트(`fetch_games.mjs`)를 다시 실행해 `games.json`을 갱신하고 화면을 새로고침하는 기능 추가.
- `subprocess`(파이썬에서 다른 프로그램 실행) 개념은 이번 학습 목표가 아니라서 Claude가 작성하고 동작 원리만 간단히 설명함.

## 5. Streamlit 화면 작업 — 팀별 선수 필터

목표: `hitters.json`(실제 KBO 선수 타율 데이터)을 팀별로 골라서 보기.

- 진단 문제 2번("team이 LG인 선수의 name만 뽑기")과 똑같은 필터링 로직을 다시 사용하되, 이번엔 고정값("LG") 대신 사용자가 화면에서 고른 팀으로 바뀌어야 하는 응용 버전.
- **1단계(파일 읽기)**와 **2단계(중복 없는 팀 리스트 만들기, `not in`으로 직접 구현)**를 힌트 없이 스스로 정확히 완성 — 2단계는 "더 어려운 보너스 방법"으로 안내했던 것인데 바로 성공.
- 3~4단계에서 오류 2개 발견 (질문으로 유도해서 스스로 수정):
  - `st.radio(...)`가 돌려주는 선택값을 변수에 담지 않음 (오늘 games.json 작업에서도 나왔던 것과 같은 패턴 — "값을 리턴하는 함수의 결과를 저장 안 함")
  - `for player in hitters:` 안에 `if` 조건이 없어서 선택한 팀과 상관없이 전체 선수가 출력되는 문제
- 최종 코드: `selected_team = st.radio(...)`로 선택값 저장, `if player["team"] == selected_team:`으로 필터링 → 정상 동작 확인.

## 6. 배포

- GitHub 공개 저장소 생성: [DongJoonLee2003/pocat-kbo-app](https://github.com/DongJoonLee2003/pocat-kbo-app)
- Streamlit Community Cloud에 연결해서 실제 배포 완료 → **라이브 주소: https://pocat-kbo-app-dongjoon2003.streamlit.app/**
- 배포 환경의 알려진 한계: "🔄 새로고침" 버튼은 `node`(Node.js)로 `fetch_games.mjs`를 실행하는데, Streamlit Cloud 서버엔 Node.js가 없어서 배포 사이트에서는 작동하지 않음. 현재는 커밋 시점(2026-09-05 기준)의 `games.json`/`hitters.json` 스냅샷이 고정으로 보임. 사용자가 "라이브로 실제 반영되게 하고 싶다"고 요청 → 4주차 최우선 후보로 확정 (아래 8번 참고).

## 6-1. 시각화(막대그래프) 시도 → 롤백

- 팀별 선수 타율을 `st.bar_chart()`로 시각화하는 기능 요청. 원래 계획대로면 "이름:타율 딕셔너리 만들기 → st.bar_chart() 호출"을 직접 짜보는 순서였으나, 사용자가 시간이 급하다고 요청 → 이번 한 번은 Claude가 대신 구현하고 시연.
- 실제 배포 사이트에서 확인해보니 사용자가 결과물 마음에 안 들어함 ("너무 별로다") → `git revert`로 해당 커밋만 안전하게 되돌리고 재배포. 현재 `app.py`는 막대그래프 이전 상태(팀별 선수 텍스트 목록만)로 복귀함.
- **다음 세션 논의**: 시각화 자체를 다른 방식(예: 다른 차트, 정렬해서 순위 형태로 보여주기 등)으로 다시 시도할지, 아니면 시각화는 보류하고 다른 기능(정렬/순위표 등)에 집중할지 사용자와 상의 필요.

## 7. 전체 선수 데이터 확보 방법 조사 (다음 세션 최우선 과제 관련)

- `kbo-results` 스킬이 속한 스킬 모음(120개+ 전체 목록 확인) 안에 KBO 선수 스탯 전용 스킬은 없음을 확인.
- 대안으로 제안된 `statiz.co.kr`은 robots.txt에서 `anthropic-ai`/`Claude-Web` 등 AI 봇을 명시적으로 차단 → 이 사이트는 스크래핑 대상에서 제외하기로 결정.
- **중요 발견**: `koreabaseball.com`의 robots.txt에도 "사전 승인 없이 자동 수집·크롤링·복제 금지" 문구와 `Disallow: /`(전체 사이트, 지정 검색엔진 4개 제외)가 명시돼 있음. 확인해보니 3주차에 만든 `fetch_hitters.py`뿐 아니라 **`kbo-game`(경기결과, 이미 배포 중)도 내부적으로 같은 사이트(`koreabaseball.com/ws/Main.asmx/...`)를 사용**하고 있어서, 이미 배포된 사이트 자체가 이 정책과 충돌하는 상태.
- KBO 공식 공개 API는 없음(검색으로 확인), 공공데이터포털(data.go.kr)에도 KBO 선수기록 데이터셋 없음. GitHub의 대안 데이터셋(`choosunsick/KBO_data`)은 2018년까지의 낡은 데이터라 실사용 어려움.
- **사용자 결정**: 작은 개인/교육용 프로젝트로 판단하고 위험을 인지한 채로 일단 이대로(koreabaseball.com 기반) 유지하기로 함. 상업적 확장이나 더 큰 규모의 배포를 고려하게 되면 이 결정을 재검토해야 함.

## 8. 오늘 새로 배운 개념 (한 줄 정리)

- **중첩 딕셔너리 접근**: `dict["key1"]["key2"]`처럼 대괄호를 두 번 이상 써서 딕셔너리 안의 딕셔너리 값을 꺼낸다
- **f-string**: `f"텍스트{변수}"` 형태로 문자열 안에 변수 값을 바로 끼워 넣는 문법. 중괄호가 없으면 그냥 글자 그대로 출력된다
- **함수가 값을 "리턴"하는지 아닌지 구분하기**: `json.load()`, `st.radio()`처럼 결과를 돌려주는 함수는 변수에 담아야 쓸 수 있고, `st.write()`/`print()`처럼 화면에 보여주기만 하는 함수는 담을 필요가 없다 — 오늘 세션에서 세 번 반복된 실수 패턴
- **subprocess (개념만)**: 파이썬 코드 안에서 다른 프로그램(Node.js 스크립트 등)을 실행시킬 수 있다는 것
- **st.button() / st.rerun()**: 버튼 클릭을 감지하고, 화면을 처음부터 다시 실행시키는 Streamlit 함수
- **st.radio()**: 선택지 중 하나를 고르는 라디오 버튼 위젯. 사용자가 고른 값을 리턴한다

## 9. 아직 이해가 부족해서 다음에 복습이 필요한 것

- "이 함수가 값을 리턴하는 함수인지 아닌지"를 코드를 실행해서 에러를 보기 전에 미리 예측하는 습관은 아직 약함 (실수 자체는 힌트 1번이면 바로 고치지만, 매번 새로운 함수를 쓸 때 같은 유형의 실수가 나옴) — 다음 세션에서 계속 지켜볼 부분
- 리스트/딕셔너리 정렬(sort) 로직은 아직 다뤄본 적 없음 (타율 높은 순으로 정렬하기 등) — 다음 주 후보 주제

## 10. 다음 시간(4주차 예정)에 할 것

- **[최우선, 사용자 확정 요청] 선수 전체 데이터 확보** — koreabaseball.com 기반 유지가 결정됐으니(7번 참고), ASP.NET postback(2페이지 이후 또는 팀별 필터)을 다시 파헤쳐서 "타율 상위 30명" 제한을 풀 방법 시도. 지난번 postback 시도가 에러 페이지로 실패했는데, 누락된 폼 필드(다른 드롭다운들의 현재 선택값)가 원인일 가능성이 있어 다시 점검 필요
- **배포된 사이트에서 "새로고침"이 실제로 라이브 반영되게 만들기** — 현재 Node.js(`fetch_games.mjs`) 의존성 때문에 Streamlit Cloud에서 안 돌아가는 문제를 해결해야 함. `fetch_games.mjs`를 파이썬 `requests`+`BeautifulSoup`(또는 다른 방법)로 재작성해서 Node 의존성을 없애는 방향으로 논의
- **시각화 방향 재논의** — 3주차에 시도한 막대그래프는 사용자 마음에 안 들어서 롤백함(6-1 참고). 다른 시각화 방식을 시도할지, 이번엔 처음부터 사용자가 직접 짜볼지 논의 필요
- 정렬 로직 연습 (예: 타율 높은 순으로 선수 정렬해서 보여주기) — pandas 없이 순수 파이썬으로 (`sorted()` + `key` 또는 직접 구현)
- 팀 순위표(승/패/승률) 추가 여부 논의 — 새 데이터 수집이 필요할 수 있음

## 11. 실력 변화 (2주차 대비)

- **인코딩(`encoding="utf-8"`) 문제는 완전히 체화됨** — 2주차엔 처음 배우는 개념이었지만, 3주차 후반(`hitters.json`)에서는 힌트 없이 스스로 먼저 적용함
- **딕셔너리 중복 제거(`not in`) 같은 응용 로직을 스스로 설계**해서 첫 시도에 정확히 구현 — 배운 문법을 새로운 상황에 조합해서 쓰는 능력이 늘어남
- **요구사항보다 더 꼼꼼하게 처리하려는 태도**가 보임 (상태 분기를 2갈래로 안내했는데 4갈래로 확장)
- 다만 "함수 반환값 저장 누락" 패턴은 여전히 반복 중 — 새로운 함수를 만날 때마다 나타나는 것으로 보아, 완전히 습관으로 자리잡으려면 몇 세션 더 필요해 보임. 다만 힌트를 주면 바로 수정하는 속도 자체는 여전히 빠름.
